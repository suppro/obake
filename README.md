# Obake - Платформа для изучения японского языка

Obake (お化け) - это веб-приложение для изучения японского языка через чтение рассказов и создание личного словаря.

## Возможности

- 📚 **Рассказы** - Чтение рассказов на японском языке разных уровней сложности (N5-N1)
- 📖 **Личный словарь** - Создание собственного словаря изучаемых слов
- 🎯 **Подсветка слов** - Слова из вашего словаря подсвечиваются в рассказах
- 💡 **Всплывающие подсказки** - Перевод слов при наведении
- 📝 **Фуригана** - Включение/выключение чтения кандзи
- 👑 **Админ панель** - Управление рассказами и словарем

## Требования

- PHP >= 8.2
- Composer
- MySQL/MariaDB
- Node.js и npm

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Obake
```

2. Установите зависимости PHP:
```bash
composer install
```

3. Скопируйте файл окружения:
```bash
cp .env.example .env
```

4. Сгенерируйте ключ приложения:
```bash
php artisan key:generate
```

5. Настройте базу данных в `.env`:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=obake
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

6. Выполните миграции:
```bash
php artisan migrate
```

7. Создайте первого администратора:
```bash
php artisan tinker
```
Затем выполните:
```php
$user = new App\Models\User();
$user->name = 'Admin';
$user->email = 'admin@example.com';
$user->password = Hash::make('password');
$user->is_admin = true;
$user->save();
```

8. Установите зависимости Node.js:
```bash
npm install
```

9. Соберите фронтенд ресурсы:
```bash
npm run build
```

10. Запустите сервер разработки:
```bash
php artisan serve
```

## Использование

### Обычный пользователь

1. Зарегистрируйтесь на сайте
2. Просматривайте рассказы по уровням
3. При наведении на слова видите перевод
4. Добавляйте слова в свой словарь
5. Просматривайте свой словарь

### Администратор

1. Войдите с правами администратора
2. Перейдите в админ панель (`/admin/dashboard`)
3. Управляйте рассказами:
   - Создавайте новые рассказы
   - Редактируйте существующие
   - Удаляйте рассказы
4. Управляйте словарем:
   - Добавляйте новые слова
   - Редактируйте переводы
   - Удаляйте слова

## Структура базы данных

- `users` - Пользователи (с полем `is_admin`)
- `stories` - Рассказы
- `global_dictionary` - Глобальный словарь (невидим для обычных пользователей)
- `user_dictionary` - Связь пользователей со словами
- `story_words` - Связь рассказов со словами

## Технологии

- **Backend**: Laravel 12
- **Frontend**: Blade Templates, Tailwind CSS, JavaScript
- **Database**: MySQL
- **Fonts**: Noto Sans JP (для японского текста)

## Лицензия

MIT License

## Автор

Obake - Проект для изучения японского языка